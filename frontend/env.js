/* Overwritten by build.sh from the VITE_API_URL environment variable.
   Left blank on purpose — the portal shows a setup banner rather than
   sending requests to its own origin. */
window.__APP_ENV__ = { API_URL: "" };
